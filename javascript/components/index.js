//Imports
import Filter from "./filter.js";

//Adding the components to an array that will be used to provoke the rendering
const componentTypes = [];
componentTypes.push(Filter);

export default componentTypes;
